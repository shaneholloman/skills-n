# n-skills - Available Skills

<available_skills>

<skill>
<name>zai-cli</name>
<description>Z.AI CLI providing vision (image/video analysis, OCR, UI-to-code), search (real-time web), reader (page to markdown), repo (GitHub exploration), tools (MCP discovery), and code (TypeScript chaining). Use for visual content analysis, web search, page reading, or GitHub exploration. Requires Z_AI_API_KEY.</description>
<location>skills/tools/zai-cli</location>
<invoke>openskills read zai-cli</invoke>
</skill>

</available_skills>

## Usage

When a skill description matches the user's intent:
1. Run the invoke command to load the skill
2. Follow the skill's instructions

Install skills via: `openskills install numman-ali/n-skills`
